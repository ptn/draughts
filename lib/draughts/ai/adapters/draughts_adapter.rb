module Draughts
  module AI
    module Adapters
      #
      # A class that talks to the Draughts engine.
      #
      # This adapter knows the formats that the Draughts engine requires for
      # input and output. It sends the bots' moves and parses the next board
      # that they have to play.
      #
      class DraughtsAdapter
      end
    end
  end
end
